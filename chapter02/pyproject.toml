[project]
name = "chapter02"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chapter01",
    "redis>=6.4.0",
]

[tool.uv.sources]
chapter01 = { path = "../chapter01", editable = true }
